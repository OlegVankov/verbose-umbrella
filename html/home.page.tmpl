<!doctype html>
<html lang='en'>
    <head>
        <meta charset='utf-8'>
    </head>
    <body>
        <ul>
        {{ with Storage }}
            {{ range $key, $val := .GetGaugeAll }}
                <li>{{ $key }}: {{ $val }}</li>
            {{ end }}

        </ul>
            <ul>
            {{ range $key, $val := .GetCounterAll }}
                <li>{{ $key }}: {{ $val }}</li>
            {{ end }}
        {{ end }}
        </ul>
    </body>
</html>