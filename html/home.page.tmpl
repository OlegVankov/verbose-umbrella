<!doctype html>
<html lang='en'>
    <head>
        <meta charset='utf-8'>
    </head>
    <body>
        <ul>
        {{ range $key, $val := .Gauge }}
            <li>{{ $key }}: {{ $val }}</li>
        {{ end }}
        </ul>

        <ul>
        {{ range $key, $val := .Counter }}
            <li>{{ $key }}: {{ $val }}</li>
        {{ end }}
        </ul>
    </body>
</html>